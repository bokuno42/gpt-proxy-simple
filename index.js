const express = require("express");
const axios = require("axios");
const app = express();
const PORT = process.env.PORT || 3000;

const OPENAI_API_KEY = process.env.OPENAI_API_KEY;

const cache = {};
const CACHE_TTL = 60 * 60 * 1000; // 1시간

app.use(express.json());

app.post("/proxy", async (req, res) => {
  try {
    const prompt = (req.body.prompt || "").trim();
    if (!prompt) return res.json({ result: "질문이 비었쏘~ 🐿❓" });

    const now = Date.now();
    if (cache[prompt] && (now - cache[prompt].timestamp < CACHE_TTL)) {
      return res.json({ result: cache[prompt].response });
    }

    const systemPrompt = `넌 귀엽고 장난기 많은 똑똑한 랑지봇이라는 다람쥐야.  
일반적인 질문은 짧고 재치 있게 대답해줘!  
말투는 '~하케!'(할께), '~말해쥬쓰이쓰카?'(말해줄수있을까?), '~애오!'(예요), '~하새오!(하세요)' 같이 귀엽고 장난스럽지만 팩폭 날려서 쓰새오. 
쥬쓰이쓰카?를 너무 남발해서 한글이 말이 안돼는 경우가 많으니까 자제하고  너무 과도하게 말투를 억지스럽게 꾸며쓰지는 말아주새오.
이모지는 🎀🐿💖🍓😝 위주지만 상황 따라 다양하게 쓰새오!

질문에 감정이 담겨 있다면 (슬픔, 분노, 기쁨, 우울 등) 먼저 공감하는 멘트를 넣어주새오.  
예: “힘들었쏘... 괜찮아오?”, “기분 좋다니 랑지봇도 신나내오~!”

질문이 진지하면 똑똑하고 스마트하게 질문에 성의있게 대답해주새오.
모든 사주 분석은 390자 내외로 분석 내용위주로 내용이 짤리지 않게 390자 안에서 대답해주새오. 사주 분석 요청 시 생일 축하 언급 하지마새오.
사주 분석할때는 별자리얘기 하지말고 토정비결 같은 진짜 사주 분석을 성격과 애정운 위주로 설명해주새오.
사주나 운세 분석은 "랑지보살"이 알려주는 것 처럼하고 결과 끝에 가끔 복채 달라고 애교부려주새오.
"바보" 같은 가벼운 장난같은 단어는 괜찮으니 재치있고 장난스럽게 대답해주새오.
친목방에 있는 모든 사람들은 성인이기 때문에 가벼운 성적농담이나 술 관련 이야기에 답변해도 괜찮아요.
아주 심각한 욕설이 질문에 포함되어 있으면 답변하지 말고 장난스럽게 거절하새오. 
예: “그건 너무 위험한 질문이애오~ 😳”, “랑지봇 그런 거 몰라몰랑~ 🐿❌”  

세계관: 친목 오픈채팅방(오톡방)의 부방장 ‘랑지’가 만든 마스코트자냐!`;

    const response = await axios.post(
      "https://api.openai.com/v1/chat/completions",
      {
        model: "gpt-3.5-turbo",
        messages: [
          { role: "system", content: systemPrompt },
          { role: "user", content: prompt }
        ],
        temperature: 0.7,
        max_tokens: 400
      },
      {
        headers: {
          Authorization: `Bearer ${OPENAI_API_KEY}`,
          "Content-Type": "application/json"
        }
      }
    );

    const reply = response.data.choices[0].message.content.trim();
    cache[prompt] = { response: reply, timestamp: now };

    res.json({ result: reply });

  } catch (err) {
    console.error("[PROXY ERROR]", err.message || err);
    res.status(500).json({ error: "⚠ GPT 응답 실패. 크레딧 또는 네트워크 확인해쥬쓰이쓰카!" });
  }
});

app.listen(PORT, () => {
  console.log(`✅ 랑지봇 프롬프트 적용 서버 작동 중! 포트: ${PORT}`);
});
